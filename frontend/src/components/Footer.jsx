import "./Footer.css";

export default function Footer() {
  return (
    <footer className="site-footer">
      <div className="container site-footer__inner">
        <span>Vendor Reconciliation Copilot</span>
        <span className="site-footer__dot">·</span>
        <span>Matching is deterministic and rule-based — every result is traceable to a rule.</span>
      </div>
    </footer>
  );
}
